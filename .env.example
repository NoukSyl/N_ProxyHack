# .env.example
# Copy this to .env for local dev.
# Railway injects these automatically — don't commit real values.

PORT=3000
HTTPS_PORT=3443

# Optional: override log level without touching config.json
# LOG_LEVEL=debug
